name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all --check
      - name: Check
        run: cargo check --workspace --locked
      - name: Clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - name: Test
        run: cargo test --workspace --locked

  deployment-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install validators
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Validate shell scripts
        run: shellcheck scripts/bts-install scripts/generate-voice-prompts.sh
      - name: Provide executable placeholders
        run: |
          for executable in bts-core bts-addons bts-telephony bts-display cage; do
            sudo install -Dm755 /bin/true "/usr/bin/$executable"
          done
      - name: Validate systemd units
        run: systemd-analyze verify deploy/systemd/*.service deploy/systemd/bts.target
